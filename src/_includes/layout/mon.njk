---
layout: layout/post.njk

tags:
  - "MON"

eleventyComputed:
  year: "{{ page.url | getYearFromUrl }}"
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ page.url | parentUrl }}"
---

{{ content | safe }}
