---
layout: layout/post.njk

tags: 
    - "POK"
    - "index"

eleventyComputed:
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ page.url | parentUrl }}"
---

{{ content | safe }}