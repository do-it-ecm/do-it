---
layout: layout/post.njk

eleventyComputed:
  tags:
    - "{{ page.url | getYearFromUrl }}"
    - "index"
    - "POK"
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ page.url | parentUrl }}"
---

{{ content | safe }}
