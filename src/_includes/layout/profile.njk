---
layout: layout/post.njk

tags:
  - "PROFILE"

eleventyComputed:
  year: "{{ page.url | getYearFromUrl }}"
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ page.url | parentUrl }}"
    studentName: "{{ title | safe }}"
---

{{ content | safe }}
