---
layout: layout/post.njk

eleventyComputed:
  tags:
    - "{{ page.url | getYearFromUrl }}"
    - "POK"
    - "temps {{ temps }}"
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ page.url | parentUrl }}"
---

{{ content | safe }}
